class Solution{
public:	
	int findKRotation(int arr[], int n) {
	    // code here
	    int a=0;
	    for(int i=1;i<n;i++)
	    {
	        if(arr[i]<arr[i-1])
	        {
	            a=i;
	        }
	    }
	    return a;
	}
};
